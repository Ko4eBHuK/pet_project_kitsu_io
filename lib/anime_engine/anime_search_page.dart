import 'package:flutter/material.dart';

class AnimeSearchPage extends StatelessWidget {
  const AnimeSearchPage({Key? key}) : super(key: key);

  //TODO develop anime branch:
  //TODO - AnimeFilters page
  //TODO - Filter page
  //TODO - AnimeList page
  //TODO - AnimeItem page

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Anime Search'),
      ),
      body: Center(
        child: Container(),
      ),
    );
  }
}

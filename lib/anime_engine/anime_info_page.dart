import 'package:flutter/material.dart';

// TODO - develop anime_info page
class AnimeInfoPage extends StatelessWidget {
  const AnimeInfoPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
